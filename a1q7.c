#include<stuio.h>
int main()
{
 int a,b,c,d,rev,face value,place value;
    #include<stdio.h>
    int main()
    {
    	int a,rev;
    	scanf("%d",&a);
        while(a>=1)
    	{
    		rev = a%10; // it gives me remainder
    		printf("%d",rev); // just print that remainder
    		a = a/10;	// divide the number for the next iteration
    	}
    }
